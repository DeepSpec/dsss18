Building with VST
-----------------

[Warning: You need to install the up-to-the-minute version of VST
master branch from here:
https://github.com/PrincetonUniversity/VST.git to do this]

In this `charIO/` directory, create a `CONFIGURE` file containing

    VST_LOC= /file/path/to/VST

where you replace `/file/path/to/VST` with the location of your VST
installation.

Then run

    make
